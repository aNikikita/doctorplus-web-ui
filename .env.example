# API Configuration
# For local development, use your local backend or a test backend
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# For production deployment, this should point to the same domain
# The proxy at /api/doctorplus will handle the actual backend communication
# NEXT_PUBLIC_API_BASE_URL=https://your-domain.vercel.app

# Backend proxy configuration (server-side only, never exposed to client)
# These variables are used by the API route to connect to the backend
DOCTORPLUS_BACKEND_URL=https://doctorplus-backend-ssz2.onrender.com
DOCTORPLUS_API_KEY=__set_in_vercel__

# PostHog Analytics (optional)
# NEXT_PUBLIC_POSTHOG_KEY=
# NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com